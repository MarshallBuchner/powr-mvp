const videoInput=document.getElementById('videoInput');const videoPreview=document.getElementById('videoPreview');const analyzeButton=document.getElementById('analyzeButton');const removeVideoButton=document.getElementById('removeVideoButton');const newAnalysisButton=document.getElementById('newAnalysisButton');const fileSummary=document.getElementById('fileSummary');const fileName=document.getElementById('fileName');const processingPanel=document.getElementById('processingPanel');const progressBar=document.getElementById('progressBar');const report=document.getElementById('report');const goalButtons=[...document.querySelectorAll('.goal-chip')];const processingSteps=[...document.querySelectorAll('.processing-step')];let selectedGoal='Overall skating';let activeVideoUrl=null;goalButtons.forEach(button=>{button.addEventListener('click',()=>{goalButtons.forEach(item=>item.classList.remove('active'));button.classList.add('active');selectedGoal=button.dataset.goal})});videoInput.addEventListener('change',()=>{const file=videoInput.files[0];if(!file){resetVideo();return}if(!file.type.startsWith('video/')){alert('Please choose a valid video file.');resetVideo();return}if(activeVideoUrl)URL.revokeObjectURL(activeVideoUrl);activeVideoUrl=URL.createObjectURL(file);videoPreview.src=activeVideoUrl;videoPreview.hidden=false;fileName.textContent=file.name;fileSummary.hidden=false;analyzeButton.disabled=false});removeVideoButton.addEventListener('click',resetVideo);analyzeButton.addEventListener('click',async()=>{if(!videoInput.files[0])return;analyzeButton.disabled=true;report.hidden=true;processingPanel.hidden=false;processingPanel.scrollIntoView({behavior:'smooth',block:'start'});resetProcessingState();const progressValues=[24,49,74,100];for(let index=0;index<processingSteps.length;index+=1){if(index>0){processingSteps[index-1].classList.remove('active');processingSteps[index-1].classList.add('complete');processingSteps[index-1].querySelector('.step-icon').textContent='✓'}processingSteps[index].classList.add('active');progressBar.style.width=`${progressValues[index]}%`;await wait(700)}processingSteps.at(-1).classList.remove('active');processingSteps.at(-1).classList.add('complete');processingSteps.at(-1).querySelector('.step-icon').textContent='✓';await wait(350);document.getElementById('reportGoal').textContent=selectedGoal;document.getElementById('reportDate').textContent=new Intl.DateTimeFormat('en-CA',{month:'long',day:'numeric',year:'numeric'}).format(new Date());document.getElementById('nextFocus').textContent=getFocusMessage(selectedGoal);processingPanel.hidden=true;report.hidden=false;analyzeButton.disabled=false;report.scrollIntoView({behavior:'smooth',block:'start'})});newAnalysisButton.addEventListener('click',()=>{report.hidden=true;resetVideo();document.querySelector('.upload-card').scrollIntoView({behavior:'smooth',block:'start'})});function resetVideo(){videoInput.value='';if(activeVideoUrl){URL.revokeObjectURL(activeVideoUrl);activeVideoUrl=null}videoPreview.removeAttribute('src');videoPreview.load();videoPreview.hidden=true;fileSummary.hidden=true;analyzeButton.disabled=true}function resetProcessingState(){progressBar.style.width='0%';processingSteps.forEach((step,index)=>{step.classList.remove('active','complete');step.querySelector('.step-icon').textContent=String(index+1)})}function wait(ms){return new Promise(resolve=>setTimeout(resolve,ms))}function getFocusMessage(goal){const messages={'Overall skating':'Create more distance from each stride before increasing foot speed.','Acceleration':'Stay low and drive through the first three strides with full extension.','Stride efficiency':'Finish each push cleanly before beginning recovery.','Crossovers':'Drive the under-push fully beneath your body through each crossover.','Backward skating':'Maintain knee bend while generating power through strong C-cuts.','Transitions':'Keep your hips low and rotate through the transition without standing up.'};return messages[goal]||messages['Overall skating']}
